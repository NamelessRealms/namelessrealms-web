# 驗收報告：{{里程碑代號}} {{任務標題}}

> Claude Code 實作完成後自動產出。協作 Claude 依此審計。
> **不得用範本/預期值/設計推理冒充已執行。** 真機做不到就誠實標「待人工」。

## 變更檔案
- {{path}} — {{改了什麼}}

## 設計重點
- {{關鍵決策與理由}}

## 測試結果
- 單元測試：{{通過數 / 實際輸出}}
- 正向流程：{{做 X → 得 Y，**貼實際輸出**}}
- 負向流程：{{非法輸入 → 乾淨錯誤，**貼實際輸出**}}
  - 還原方式：{{備份檔（⛔ 不用 `git checkout`）}}

## 審計確認
- {{該寫的 audit/log 有寫、actor 記對（user/system）、不含敏感原值}}

## 產物重建
- [ ] 改過原始碼 → 已跑 `yarn build`（附輸出）
- [ ] 跨 repo 依賴：改被依賴方 → 已在**依賴方端**重建確認拉到新碼（附輸出）
- [ ] 或：無原始碼變更，沿用現有產物

## git 對帳
```
git log --oneline -1                    → {{}}
git status                              → {{clean?}}
git rev-parse HEAD origin/developers  → {{本地=遠端?（未 push 前據實標「尚未 push」）}}
```

## CI
- 本地：`yarn lint --max-warnings 0 && yarn build` → {{green?}}
- ⚠️ **本專案沒有 build/lint CI**(唯一 workflow 只在 tag `v*.*.*` 時 build image)。
  ⇒ 收案標準 = **本地嚴格指令逐字跑過並附輸出**,⛔ 不得寫「等 remote Actions 綠」。
  ⚠️ 打 `v*.*.*` tag 就是發版,⛔ 不得為測試打 tag。

## 真機 E2E（若涉 UI / 真機行為）
> 完整步驟表寫在這（供日後複驗）；實際執行以協作 Claude 逐步領路為準。
> ⚠️ **一次一步**（架構師裁決 2026-08-03）：素材（fixture）由規劃側**事先備好並交付**，
> ⛔ 不得只給步驟叫架構師自備；開跑後**一次只給一個步驟**，**等回報結果才給下一步**，
> ⛔ 不得一次貼整份步驟表。
| # | 步驟 | 過線標準 | 結果 |
|---|------|----------|------|
| 1 | {{}} | {{}} | {{待人工}} |

## 回歸守門
- {{既有功能未壞的證據}}

## 守界聲明
- {{確認只做了任務包範圍、未超前、未違反鐵則}}
- carryover（若有）：{{這階段沒解、留給後續的東西}}
- ⛔ **尚未 commit/push，等待架構師確認**（或據實記錄已獲確認的時點）。
