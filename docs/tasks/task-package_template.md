# 任務包：{{里程碑代號 例 M2-3}} {{任務標題}}

> 協作 Claude 產出、直接寫入 repo `docs/tasks/{代號}.md`。
> ⛔ **必須自我完備**：不得引用任何知識庫路徑或要求 Claude Code 去讀知識庫檔——
> 規劃文件的內容需要被知道時，直接抄進本包。違反此條責任在撰寫者。

## 追溯資訊
- **日期**：{{YYYY-MM-DD}}
- **動哪些 repo**：{{例：僅 Nymless / A + B（先 A 後 B）}}
- **前置依賴**：{{例 M2-2 已驗收（此任務建立在其產出之上）/ 無}}
- **引用 spec 章節**：{{例 第 05、07 章（背景切片出處，供日後反查；內容已抄入下方背景區）}}

## 目標
{{一段話：這個子任務要做出什麼、做完長什麼樣}}

## 範圍（只做這些）
- {{}}
- {{}}

## 不做什麼（明確守界）
> 📝 這區跟「範圍」一樣重要——寫清楚哪些相鄰的東西**這次不碰**，
> 防 Claude Code 順手多做、超前別的里程碑。
- {{不碰 X 模組}}
- {{不建 Y 表（留給 M?）}}

## 背景（規格切片）
> 只貼當前任務直接相關的章節段落，不貼整章。鐵則走 CLAUDE.md 常駐，不重貼。
{{從 spec 第 ? 章抄相關段落（抄內容，不留知識庫路徑）}}

## 要建的表 / 資源
> 用到才建。只列這任務需要的。
{{ migration / schema，或「無」}}

## 實作要點
- {{重用既有的 {{抽象/trait/模組}}，不重寫}}
- {{相關鐵則提醒：這任務要特別守的那幾條}}
- {{若為結構性變更（新增/移動模組、新路由群、新資料表、分層調整）：
  固定附一條「更新該 repo `CLAUDE.md` 程式碼地圖對應段落」；非結構變更刪除本條}}

## 驗收步驟（正向 + 負向都要）
- 正向：{{做 X → 應得 Y，附實際輸出}}
- 負向：{{給非法輸入 → 應乾淨回錯 Z，不 crash / 不 5xx}}
  - 負向測試的還原一律用**備份檔**，⛔ 不用 `git checkout`（會清掉未 commit 的正式改動）。
- 審計：{{該寫的 audit/log 有寫、記對 actor}}
- 回歸：{{既有功能未壞}}

## 流程提醒
1. 先落檔 `{代號}-plan.md`，**等 `{代號}-plan-review.md` 放行才實作**。
2. 實作完成產 `{代號}-verification.md`（見 verification_template.md），不必等人要求。
3. ⛔ **commit/push 前回報，待架構師確認**。本包任何措辭均不構成 push 預授權。
